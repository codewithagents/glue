// This file is auto-generated by @codewithagents/openapi-gen — do not edit

import type { CreatePetRequest, Pet } from './models.js'
import { getConfig, type ClientConfig } from './client-config.js'
import { z } from 'zod'
import { CreatePetRequestSchema, PetSchema } from './schemas.js'

export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly body: unknown,
  ) {
    super(`API error ${status}`)
    this.name = 'ApiError'
  }
}

export async function listPets(params?: {
  species?: string
}, config?: Partial<ClientConfig>): Promise<Pet[]> {
  const { baseUrl, token, credentials, headers, onError } = { ...getConfig(), ...config }
  const base = baseUrl ? baseUrl.replace(/\/$/, '') : ''
  const fullUrl = `${base}/pets`
  const searchParams = new URLSearchParams()
  if (params?.species != null) searchParams.set('species', String(params.species))
  const qs = searchParams.toString()
  const finalUrl = qs ? `${fullUrl}?${qs}` : fullUrl
  const resolvedToken = typeof token === 'function' ? await token() : token
  const res = await fetch(finalUrl, {
    method: 'GET',
    credentials,
    headers: {
      ...headers,
      ...(resolvedToken ? { Authorization: `Bearer ${resolvedToken}` } : {}),
    },
  })
  if (!res.ok) {
    const err = new ApiError(res.status, await res.json().catch(() => null))
    onError?.(err)
    throw err
  }
  return z.array(PetSchema).parse(await res.json())
}

export async function createPet(body: CreatePetRequest, config?: Partial<ClientConfig>): Promise<Pet> {
  const { baseUrl, token, credentials, headers, onError } = { ...getConfig(), ...config }
  const base = baseUrl ? baseUrl.replace(/\/$/, '') : ''
  const fullUrl = `${base}/pets`
  const finalUrl = fullUrl
  const resolvedToken = typeof token === 'function' ? await token() : token
  CreatePetRequestSchema.parse(body)
  const res = await fetch(finalUrl, {
    method: 'POST',
    credentials,
    headers: {
      'Content-Type': 'application/json',
      ...headers,
      ...(resolvedToken ? { Authorization: `Bearer ${resolvedToken}` } : {}),
    },
    body: JSON.stringify(body),
  })
  if (!res.ok) {
    const err = new ApiError(res.status, await res.json().catch(() => null))
    onError?.(err)
    throw err
  }
  return PetSchema.parse(await res.json())
}

export async function getPet(id: string, config?: Partial<ClientConfig>): Promise<Pet> {
  const { baseUrl, token, credentials, headers, onError } = { ...getConfig(), ...config }
  const base = baseUrl ? baseUrl.replace(/\/$/, '') : ''
  const fullUrl = `${base}/pets/${encodeURIComponent(id)}`
  const finalUrl = fullUrl
  const resolvedToken = typeof token === 'function' ? await token() : token
  const res = await fetch(finalUrl, {
    method: 'GET',
    credentials,
    headers: {
      ...headers,
      ...(resolvedToken ? { Authorization: `Bearer ${resolvedToken}` } : {}),
    },
  })
  if (!res.ok) {
    const err = new ApiError(res.status, await res.json().catch(() => null))
    onError?.(err)
    throw err
  }
  return PetSchema.parse(await res.json())
}

export async function deletePet(id: string, config?: Partial<ClientConfig>): Promise<void> {
  const { baseUrl, token, credentials, headers, onError } = { ...getConfig(), ...config }
  const base = baseUrl ? baseUrl.replace(/\/$/, '') : ''
  const fullUrl = `${base}/pets/${encodeURIComponent(id)}`
  const finalUrl = fullUrl
  const resolvedToken = typeof token === 'function' ? await token() : token
  const res = await fetch(finalUrl, {
    method: 'DELETE',
    credentials,
    headers: {
      ...headers,
      ...(resolvedToken ? { Authorization: `Bearer ${resolvedToken}` } : {}),
    },
  })
  if (!res.ok) {
    const err = new ApiError(res.status, null)
    onError?.(err)
    throw err
  }
}
